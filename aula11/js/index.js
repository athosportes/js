const primeiroValor = prompt('Digite o primeiro valor da soma');
const segundaoValor = prompt('Digite o segundo valor da soma');
const soma = Number(primeiroValor) + Number(segundaoValor)

alert(`A soma é = ${soma}`)
